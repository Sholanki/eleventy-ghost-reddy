<!DOCTYPE html>
<html lang="{{ site.lang }}" class="no-js">
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title>
            {%- if title -%}
                {{ title }} – {{ site.title }}
            {%- else -%}
                {{ site.title }} – {{ site.description }}
            {%- endif -%}
        </title>
        <meta name="HandheldFriendly" content="True">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" type="text/css" href="/assets/built/screen.css">
        {% if site.icon %}
            <link rel="shortcut icon" type="image/png" href="{{ site.icon }}"/>
        {% endif %}
        <link rel="canonical" href="{{ site.url + page.url }}">
        <meta name="referrer" content="no-referrer-when-downgrade">

        {%- block seo -%}
        <!-- Facebook OpenGraph -->
        <meta property="og:site_name" content="{{ site.title }}">
        <meta property="og:type" content="website">
        <meta property="og:title" content="{{ title + " – " + site.title if title else title }}">
        <meta property="og:description" content="{{ site.description }}">
        <meta property="og:url" content="{{site.url + page.url}}">
        <meta property="og:image" content="{{ site.cover_image }}">
        <meta property="og:locale" content="{{ site.lang }}">
        <meta property="article:author" content="{{ site.url }}">

        <!--Twitter Card-->
        <meta name="twitter:card" content="summary_large_image">
        <meta name="twitter:title" content="{{ title + " – " + site.title if title else title }}">
        <meta name="twitter:description" content="{{ site.description }}">
        <meta name="twitter:url" content="{{ site.url + page.url }}">
        <meta name="twitter:image" content="{{ site.cover_image }}">
        <meta name="twitter:site" content="{{ site.twitter }}">
        <meta name="twitter:creator" content="{{ site.twitter }}">

        <!--Schema-->
        <link rel="author" href="{{ site.url }}">
        <link rel="publisher" href="{{ site.url }}">
        <meta itemprop="name" content="{{ title + " – " + site.title if title else title }}">
        <meta itemprop="description" content="{{ site.description }}">
        <meta itemprop="image" content="{{ site.cover_image }}">
        {%- endblock -%}

        <meta name="description" content="{% block description %}{{ site.description }}{% endblock %}">
        <meta name="robots" content="index,follow">
        <meta name="generator" content="Eleventy">

        <script>document.documentElement.classList.remove('no-js');</script>

        {{ site.codeinjection_head | safe if site.codeinjection_head }}
        {{ codeinjection_head | safe if codeinjection_head }}
    </head>
    <body class="{% block body_class %}home-template{% endblock %}">
        <div class="site-wrapper">

            {% block header %}
                {% include "partials/header.njk" %}
            {% endblock %}
            
            
            <div class="reading-progress-bar" style="
        margin: 0;
        padding: 0;
        border: 0;
        outline: 0;
        font-size: 100%;
        vertical-align: baseline;
        background: black;
        text-align: center;
    "><h2 class="bottomHeader" style="color:#fff;float: left;width: 75%;">This is introduction text. Hello My name is Dr. Ritesh Reddy. I write for my personal catharsis, but eventually, some of it percolates into this daily newsletter and blog. I believe in the power of play as a medium of learning and growth. And if you don’t believe that, it’s at least sure to make you enjoy this vast ocean of infinite experience we’re swimming through. Come on in, the water’s just fine. Let’s play!

 </h2> <img class="imgclass" src="/img/ritesh-reddy.jpg" style="display: inline-block;float: left;width: 20%;" /> </div>
 
	       
            <main id="site-main" class="site-main outer">
                <div class="inner">
                    {% block content %}
                    {% endblock %}
                </div>
            </main>
			<div>
			{% include "layouts/iframe.html" %}
			</div>

            {% include "partials/footer.njk" %}
            {% include "partials/subscribe.njk" %}

            <!--Scripts-->
            {% include "partials/scripts-jquery.njk" %}
            {# include "partials/scripts-casper.njk" #}
            {% include "partials/scripts-subscribe.njk" %}
            {% block scripts %}{% endblock %}

            {{ site.codeinjection_foot | safe if site.codeinjection_foot }}
            {{ codeinjection_foot | safe if codeinjection_foot }}
			
        </div>
    </body>
</html>
